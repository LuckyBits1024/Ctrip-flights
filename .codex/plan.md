# 实施计划

# 当前状态
已完成初步调研、拆解与 lyx 确认。现在进入执行阶段。

# 高层阶段
1. 需求确认
   - 依赖：调研记录。
   - 执行者：主 Agent。
   - 输入：`.codex/discovery.md`。
   - 输出：lyx 确认后的需求边界，记录到 `.codex/decisions.md`。
   - 验收：日期、目的地列表、往返组合规则、回程日期上界、浏览器、虚拟环境 Python、邮箱方式明确。

2. 环境配置
   - 依赖：阶段 1。
   - 执行者：主 Agent。
   - 输入：README、`requirements.txt`、本机 Python。
   - 输出：项目内 `.venv`。
   - 验收：不使用系统 Python，`.venv/bin/python -m pip --version` 与依赖 import 检查通过。

3. 代码调整
   - 依赖：阶段 1。
   - 执行者：主 Agent，必要时派发子 Agent 阅读运行逻辑。
   - 输入：`ctrip_flights_scraper_V3.py`。
   - 输出：目标路线、日期、中转保留、浏览器、结果汇总/邮件发送流程符合确认需求。
   - 验收：代码检查确认不再无条件移除中转航班，路线日期配置正确；若确认查往返，则页面模式和数据输出支持往返。

4. 浏览器与运行调试
   - 依赖：阶段 2、阶段 3。
   - 执行者：主 Agent；如 WebDriver/浏览器问题复杂，派发子 Agent 调研驱动匹配。
   - 输入：项目虚拟环境、浏览器状态。
   - 输出：脚本能启动并进入携程查询流程。
   - 验收：能实际访问携程并生成至少一个目标 CSV，或明确停在需要 lyx 人工操作的登录/验证码。

5. 全量查询与结果沉淀
   - 依赖：阶段 4。
   - 执行者：主 Agent。
   - 输入：确认后的航线与日期范围。
   - 输出：CSV 结果文件。
   - 验收：目标日期/路线结果落盘，失败项有清晰记录。

6. 邮件发送
   - 依赖：阶段 5。
   - 执行者：主 Agent，使用本地邮件能力。
   - 输入：结果摘要、CSV 附件、lyx 确认的邮箱地址。
   - 输出：邮件草稿或已发送邮件。
   - 验收：发送前展示预览并获得 lyx 明确确认；发送后记录状态。

# 执行拆解
1. 环境与依赖
   - 依赖：lyx 确认回程日期上界。
   - 执行者：主 Agent。
   - 输入：`requirements.txt`、uv Python `3.12.13`。
   - 输出：项目内 `.venv` 和依赖安装结果。
   - 验收：`.venv/bin/python` 为 Python 3.12.x，关键依赖 `magic`、`pandas`、`seleniumwire`、`selenium` 可导入。

2. 浏览器启动链路
   - 依赖：环境与依赖。
   - 执行者：主 Agent，可派发子 Agent 做只读排查。
   - 输入：Chrome `143.0.7499.110`、Selenium Manager。
   - 输出：Chrome WebDriver 可启动并访问携程首页。
   - 验收：不使用 Edge，WebDriver 初始化成功。

3. 代码最小改造
   - 依赖：需求确认。
   - 执行者：主 Agent。
   - 输入：`ctrip_flights_scraper_V3.py`。
   - 输出：Chrome 启动、目的地列表、出发/回程日期组合、往返页面选择、保留中转航班、结果路径和邮件摘要。
   - 验收：不再无条件删除中转航班；输出文件名能区分出发日期、回程日期和路线；邮件发送前有预览。

4. 低频试跑
   - 依赖：环境、浏览器、代码改造。
   - 执行者：主 Agent。
   - 输入：1 个城市、1 个满足条件的日期组合。
   - 输出：一个 CSV 或明确阻塞在登录/验证码。
   - 验收：试跑日志追加到 `.codex/execution.md`；不高频刷新。

5. 全量查询
   - 依赖：试跑通过。
   - 执行者：主 Agent。
   - 输入：确认后的全部城市和日期组合：22 个目的地、28 个往返日期组合，共 616 个组合。
   - 输出：CSV 结果文件和失败清单。
   - 验收：覆盖所有目标组合；失败项有日志与原因。

6. 结果校验与邮件
   - 依赖：全量查询。
   - 执行者：主 Agent，可派发子 Agent 做 CSV 只读校验。
   - 输入：生成的 CSV。
   - 输出：结果摘要、邮件预览、附件清单；lyx 确认后发送。
   - 验收：发送前获得 lyx 明确确认，发送结果写入 `.codex/execution.md`。

# 子 Agent 派发表

| 子任务 | 依赖 | 拟派发对象 | 输入 | 输出 | 验收 |
| --- | --- | --- | --- | --- | --- |
| 开口程 UI/runner 链路只读审阅 | 开口程代码实现 | 子 Agent A / Explorer | `ctrip_flights_scraper_V3.py`、`run_flight_job.py` | 多程入口、城市输入、日期控件、排序、按钮、停机逻辑的问题清单 | 不改文件，不运行浏览器，返回文件/行号和建议修复 |
| CSV 输出与提交边界只读审阅 | 开口程代码实现 | 子 Agent B / Explorer | `ctrip_flights_scraper_V3.py`、`run_flight_job.py`、`.gitignore` | raw/group CSV、按价格降序、敏感文件与结果文件边界的问题清单 | 不改文件，不运行浏览器，返回文件/行号和建议修复 |
| 开口程代码修复 | 子 Agent 审阅结果 | 主 Agent | 子 Agent A/B 反馈、现有代码 | 最小必要修复 | 编译验证通过，`.codex/execution.md` 记录结果 |

# 暂不执行
- 不发送邮件，直到全量查询后展示预览并获得 lyx 二次确认。
- 不执行 `git commit` 或 `git push`。
